import styled from "styled-components";

const Container = styled.div`
  display: flex;
  justify-content: center;
  flex-direction: column;
`;
export const Info = () => {
  return (
    <Container>
      <div>How to play sequence memory etc...</div>
    </Container>
  );
}